import { CanPurchaseDirective } from './can-purchase.directive';

describe('CanPurchaseDirective', () => {
  it('should create an instance', () => {
    const directive = new CanPurchaseDirective();
    expect(directive).toBeTruthy();
  });
});
