import { UnavailableDirective } from './unavailable.directive';

describe('UnavailableDirective', () => {
  it('should create an instance', () => {
    const directive = new UnavailableDirective();
    expect(directive).toBeTruthy();
  });
});
